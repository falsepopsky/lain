{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "//#lint": {
      "outputs": []
    },
    "clean:build": {
      "cache": false
    },
    "clean:workspace": {
      "cache": false
    }
  }
}
